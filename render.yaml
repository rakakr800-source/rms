services:
  - type: web
    name: rms
    runtime: python

    buildCommand: pip install -r requirements.txt && python run_schema_update.py

    startCommand: gunicorn run:app

    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4

      - key: SECRET_KEY
        generateValue: true